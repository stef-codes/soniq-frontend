Logic for getting songs 

componentDidMount() {

    if artist 
      entity = "musicArtist"
      else
    let song = "musicTrack"

    //search artist 
    // search song
    // hold musicArtist/Track in name on form
    term = input
      fetch('https://itunes.apple.com/search?term=kanye+west&entity=musicArtist')
      .then(response => response.json())
      .then(song => console.log(song)) 
    }

kind = "song"
maxLength required - in form input?
Create an error page for routes that aren't found?

Line 58 on Search 
 {// if show results is true render result list click and take to a show page to add it to playlist}

                {/* <div>
                    {searchResults.map((result, i) => 
                    <div key={result.trackId || i}> {result.trackName}
                    </div>)}
                </div> */}


Use ReactPlayer on the show page for a selected song 
import ReactPlayer from 'react-player'
import { PlayerIcon } from 'react-player-controls'
                                    <ReactPlayer url={result.previewUrl} playingfalse>
                            <button>Play</button>
                            <button>Pause</button>
                            <PlayerIcon.Play width={32} height={32} style={{ marginRight: 32 }} />
                            <PlayerIcon.Pause width={32} height={32} style={{ marginRight: 32 }} />
                        </ReactPlayer>